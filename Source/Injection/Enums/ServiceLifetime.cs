namespace NocInjector
{
    /// <summary>
    /// Specifies the lifetime of a registered service.
    /// </summary>
    public enum ServiceLifetime
    {
        Singleton,
        Transient
    }
}
